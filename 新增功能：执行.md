
## 16. 开发执行版（按项目结构拆分）

### 16.1 Model 改动清单（数据层）

#### 文件：`src/models/task_note_link.py`（新增）
- [ ] 新建 `TaskNoteLink` 模型：`id / task_id / note_id / created_at`
- [ ] 增加唯一约束：`UniqueConstraint('task_id', 'note_id', name='uq_task_note')`
- [ ] 增加索引：`Index('ix_task_note_task_id', 'task_id')`、`Index('ix_task_note_note_id', 'note_id')`
- [ ] 外键约束：`task_id -> tasks.id`、`note_id -> notes.id`

#### 文件：`src/models/task.py`
- [ ] 增加与 `TaskNoteLink` 的关系（如 `note_links`)
- [ ] 删除 Task 时联动删除 link（仅删关系，不删 note）

#### 文件：`src/models/note.py`
- [ ] 增加与 `TaskNoteLink` 的关系（如 `task_links`)
- [ ] 明确软删除字段过滤策略（关联查询默认排除 deleted=True）

#### 文件：`src/models/database.py`
- [ ] 确保 `TaskNoteLink` 在 `Base.metadata` 中被注册
- [ ] `init_db()` 可自动创建新表（兼容旧库）

**Model DoD**
- [ ] 旧数据库启动后自动补齐 `task_note_links`，不影响已有 task/note 数据
- [ ] 重复插入相同 `(task_id, note_id)` 会被数据库层阻止

### 16.2 Service 改动清单（业务层）

#### 文件：`src/services/link_service.py`（新增）
- [ ] `link(task_id, note_id) -> bool`
- [ ] `unlink(task_id, note_id) -> bool`
- [ ] `get_notes_for_task(task_id, include_deleted=False) -> list[Note]`
- [ ] `get_tasks_for_note(note_id) -> list[Task]`
- [ ] `count_notes_for_task(task_id) -> int`
- [ ] `batch_link(task_id, note_ids)`（P2，可先占位）

#### 文件：`src/services/note_service.py`
- [ ] 新增 `create_for_task(task_id, title_hint=None)`
- [ ] 处理“空便签不落库”规则：仅在有内容时保存

#### 文件：`src/services/task_service.py`
- [ ] 删除 task 时调用 link 清理逻辑（或由 FK 级联）
- [ ] 提供 `get_task_link_count(task_id)`（供 UI 徽标显示）

#### 文件：`src/services/filter_service.py`（可选）
- [ ] 支持“仅查看有关联便签的任务”（P2）

**Service DoD**
- [ ] 所有关联查询默认不返回软删除 note
- [ ] 并发重复关联不会抛未处理异常（转换为“已关联”业务结果）

### 16.3 UI 改动清单（界面层）

#### 文件：`src/ui/task_item.py`
- [ ] 右键菜单新增：`📝新建关联便签`、`🔗关联已有便签…`、`📎查看关联便签 (N)`
- [ ] 任务卡片增加 `📎N` 徽标（N>99 显示 99+）
- [ ] 触发信号传给 `MainWindow` 统一处理

#### 文件：`src/ui/main_window.py`
- [ ] 连接 TaskItem 新信号并调用 `LinkService`
- [ ] 实现 `on_create_linked_note(task_id)`：打开 `FloatingNoteWindow`
- [ ] 实现 `on_view_linked_notes(task_id)`：弹出关联列表对话框
- [ ] 关联变更后刷新任务列表计数与当前 note 面板

#### 文件：`src/ui/floating_note.py`
- [ ] 支持接收 `task_id`/`task_title` 初始参数
- [ ] 标题默认填充模板（如 `[任务标题] `）
- [ ] 保存成功后触发 `linked_note_saved(task_id, note_id)` 信号

#### 文件：`src/ui/note_panel.py`
- [ ] 标题栏新增 `🔗 关联任务` 按钮
- [ ] 弹出“关联任务管理”对话框（已关联列表 + 搜索 + 解除）
- [ ] 点击任务项可跳转（首版先切换任务页+滚动可见）

#### 文件：`src/ui/dialogs/link_dialog.py`（新增，建议）
- [ ] `TaskLinkedNotesDialog`：任务侧查看关联便签
- [ ] `NoteLinkedTasksDialog`：便签侧查看关联任务
- [ ] 统一空态文案、错误提示、解除确认

**UI DoD**
- [ ] 从任务创建关联便签 <= 1 次点击完成
- [ ] 关联/解除后 1 秒内 UI 数量和列表一致
- [ ] 目标任务/便签不存在时有 toast，不崩溃

### 16.4 Test 改动清单（测试层）

#### 文件：`tests/test_phase8_task_note_linking.py`（新增）
- [ ] `test_link_create_success()`：建立关联成功
- [ ] `test_link_duplicate_blocked()`：重复关联被阻止
- [ ] `test_unlink_success()`：解除关联成功
- [ ] `test_delete_task_cleans_links_only()`：删 task 仅清 link
- [ ] `test_soft_deleted_note_hidden_from_task_links()`：软删 note 默认不可见
- [ ] `test_restore_note_links_visible_again()`：恢复后重新可见
- [ ] `test_create_linked_note_empty_not_persisted()`：空便签不落库

#### 文件：`tests/test_phase3.py`（UI 回归补充）
- [ ] 增加任务项上下文菜单 action 可见性断言

#### 文件：`tests/test_phase5.py`（行为回归补充）
- [ ] 增加浮动窗口保存后主界面计数刷新断言

**Test DoD**
- [ ] 新增测试全部通过
- [ ] 原有 phase1~phase7 不回归

### 16.5 执行顺序（建议）
1. **Step A（Model）**：先落表结构和唯一约束
2. **Step B（Service）**：把 link CRUD 与过滤策略稳定下来
3. **Step C（UI-任务侧）**：先实现任务右键 + 查看关联列表（MVP-S1）
4. **Step D（UI-便签侧）**：再做 note 侧关联管理（MVP-S2）
5. **Step E（Test）**：补 phase8 + 回归 phase3/5

### 16.6 本次开发的完成定义（Release DoD）
- [ ] 用户可从任务一键创建关联便签（浮动窗口）
- [ ] 用户可在任务侧查看/解除关联
- [ ] 软删除与恢复规则与 PRD 一致
- [ ] 所有自动化测试通过，且无已知崩溃路径